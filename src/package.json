{
  "name": "InferencePortAI",
  "version": "2.8.0",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "node scripts/clean.js",
    "watch": "tsc --watch",
    "compile": "npx tsgo",
    "start": "npm run clean && npm run compile && npx electron . && npm run clean",
    "build": "npm run clean && npm run compile && npm run prepack && npx electron-builder --config build.config.js --publish never && npm run clean",
    "publish": "npm run clean && npm run compile && npm run prepack && npx electron-builder --config build.config.js --publish always && npm run clean",
    "prepack": "node scripts/bundle-ollama.js"
  },
  "keywords": [],
  "type": "module",
  "author": "Rihaan Meher <sharktidedev@gmail.com>",
  "homepage": "https://inference.js.org",
  "license": "Apache 2.0",
  "description": "AI, anywhere",
  "dependencies": {
    "@gradio/client": "^2.1.0",
    "@supabase/supabase-js": "^2.100.0",
    "fix-path": "^5.0.0",
    "html-to-text": "^9.0.5",
    "markdown-it": "^14.1.1",
    "markdown-it-footnote": "^4.0.0",
    "markdown-it-multimd-table": "^4.2.3",
    "markdown-it-table": "^4.1.1",
    "openai": "^6.32.0",
    "sanitize-html": "^2.17.1",
    "systeminformation": "^5.31.1"
  },
  "devDependencies": {
    "@types/dompurify": "^3.2.0",
    "@types/html-to-text": "^9.0.4",
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^25.5.0",
    "@types/sanitize-html": "^2.16.1",
    "@types/tar": "^7.0.87",
    "@typescript/native-preview": "^7.0.0-dev.20260324.1",
    "electron": "^41.0.3",
    "electron-builder": "^26.8.1",
    "glob": "^13.0.6",
    "typescript": "^6.0.2"
  }
}
