# UUID Generator (PowerShell)

## What it does
Generate a random UUID

## Code file
- `uuid_generator.ps1`

## Assistant inputs (tool arguments)
- `count` (optional)

## Example invocation
`--count 5` (Generates 5 UUIDs)
